import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#36122C",
      contrastText: "#fff",
    },
    secondary: {
      main: "#BB7B3E",
      contrastText: "#000",
    },
  },
});

export default theme;
