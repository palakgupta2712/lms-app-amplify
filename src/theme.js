import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#12355B",
      contrastText: "#fff",
    },
    secondary: {
      main: "#DDB92A",
      contrastText: "#000",
    },
  },
});
export default theme;
